<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/moreVertical"
        android:icon="@drawable/vertmore"
        android:title="@string/title_more"
        app:showAsAction="ifRoom">
        <menu>
            <item
                android:id="@+id/topmenuSecurelogin"
                android:title="@string/securelogin_header"
                android:icon="@drawable/ic_securelogin"
                android:onClick="securelogin" />

            <item
                android:id="@+id/topmenuSettings"
                android:title="@string/title_activity_settings"
                android:icon="@drawable/ic_settings"
                android:onClick="settings" />

            <item
                android:id="@+id/topmenuComingsoon"
                android:title="@string/title_comingsoon"
                android:icon="@drawable/ic_moreicon"
                android:onClick="comingsoon" />

            <item
                android:id="@+id/topmenuHelp"
                android:title="@string/title_activity_help"
                android:icon="@drawable/ic_help"
                android:onClick="help" />
        </menu>
    </item>
</menu>